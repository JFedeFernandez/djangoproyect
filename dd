 if request.method == 'GET':
    return render(request, 'editar_animal.html',{
      'animales':animales,
      'vacunacion':vacunacion,
      'paricion':paricion,
      'ult_p':ult_p,
      'ult_v':ult_v,
      'id_a':animal,
      'description':desc
    })
  else:
    # Editamos el animal, con lo que esta en el campo del html
    return render(request, 'editar_animal.html',{
      'animales':animales,
      'vacunacion':vacunacion,
      'paricion':paricion,
      'ult_p':ult_p,
      'ult_v':ult_v,
      'id_a':animal
    })